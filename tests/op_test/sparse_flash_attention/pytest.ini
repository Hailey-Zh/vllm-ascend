[pytest]
markers =
    ci: mark a test as a CI test
    graph: marks tests as graph mode compilation tests
    npu_only: mark a test as NPU-only execution without CPU golden comparison
    step3a_guard: transitional guard test for step 3a (sparse_indices=None should be rejected by tiling); will be replaced by real dense correctness tests in step 3b
